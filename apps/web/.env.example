# Next.js public values
NEXT_PUBLIC_SITE_URL=http://localhost
NEXT_PUBLIC_DEFAULT_LOCALE=de
NEXT_PUBLIC_SUPPORTED_LOCALES=de,en,ar
NEXT_PUBLIC_DIRECTUS_URL=http://cms.localhost

# Server-side Directus values
DIRECTUS_URL=http://directus:8055
DIRECTUS_REVALIDATE_SECONDS=300

# Keep empty unless needed server-side; never expose in client code
DIRECTUS_STATIC_TOKEN=
