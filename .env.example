# Global compose variables (safe placeholders)
PUBLIC_WEB_DOMAIN=localhost
PUBLIC_CMS_DOMAIN=cms.localhost

# Public URLs
NEXT_PUBLIC_SITE_URL=http://localhost
DIRECTUS_PUBLIC_URL=http://cms.localhost

# Optional runtime profile
APP_ENV=development
